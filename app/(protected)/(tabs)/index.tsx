import { View, Text} from'react-native';

export default function Dashboard() {
    return (
        <View className="flex flex-1 items-center justify-center">
            <Text className = 'text-3xl font-thin text-foreground'>Tab [Dashboard] </Text>
        </View>
    );
}